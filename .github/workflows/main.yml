name: Create/update tag
on:
  push:
    branch: "main"
jobs:
  create-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: sourav-crossml/full_stack_training@v1
        with:
          tag: "latest"
          message: "Latest release"
